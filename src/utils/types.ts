export type ColorScheme = {
  primary: string;
  secondary: string;
  textPrimary: string;
  textSecondary: string;
  link: string;
  border: string;
};

export type NotificationColorScheme = {
  success: string;
  warning: string;
  error: string;
  info: string;
};
